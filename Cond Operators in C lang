*******************************************************************************
#include<stdio.h>

int equalornot(int a,int b){
    if(a==b)
        {
            return 1;
        }
    else
        {
            return 0;
        }
}
int main()
{   
    int result;
    result=equalornot(9,2);
    if(result==1)
        printf("Both are equal");
    else
        printf("Not equal");
}


#include<stdio.h>

int eveorodd(int a){
    if(a%2==0)
        {
            return 1;
        }
    else
        {
            return 0;
        }
}
int main()
{   
    int result;
    result=eveorodd(80);
    if(result==1)
        printf("Even");
    else
        printf("Odd");
}


#include<stdio.h>

int negposzero(int a){
    if(a>0)
        {
            return 2;
        }
    else if(a=0)
        {
            return 1;
        }
    else
        {
            return 0;
        }
}
int main()
{   
    int result;
    result=negposzero(0);
    if(result==2)
        printf("The number is positive");
    else if(result==1)
        printf("The number is equal to zero");
    else
        printf("The number is negative");
}


5.if and else statement to compare the age
6.4th problem insteat of statement print the required number
7.limint in operation like (x>=123) && (x<=173) using if and else if 
8.nested if else statements and display of results
9.using && and if else if -conditions
10.nested if statements,don't get into complexity
11.put the data into the formula and compare using if elseif and give the printf output
12.using strcpy out put the strings
13. if else if and else comparision with limts
14.using or oprator || 
15.if else just chech the condition asked in the question
16.17.
18.fi else statement as per the question
19.
20.declare grade as char and assign print values
